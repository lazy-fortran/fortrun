# Module index configuration
# This file is optional - the system will use automatic resolution if a module is not listed here

[modules]
# Explicit module mappings (override automatic resolution)
# Example: some_special_module = "specific-package"

[prefixes]
# Custom prefixes that map to packages
fortplot = "fortplotlib"